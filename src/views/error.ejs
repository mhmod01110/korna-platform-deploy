<%- contentFor('content') %>

<div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        <% if (locals.title) { %>
          <%= title %>
        <% } else { %>
          عذرا! حدث خطأ ما
        <% } %>
      </h2>
      <div class="mt-4 text-center text-md text-gray-600">
        <% if (locals.msg) { %>
          <%= msg %>
        <% } else { %>
          حدث خطأ غير متوقع. يرجى المحاولة مرة أخرى لاحقا.
        <% } %>
      </div>
    </div>

    <% if (process.env.NODE_ENV === 'development' && locals.error) { %>
      <div class="mt-8 bg-red-50 p-4 rounded-md">
        <div class="text-sm text-red-700">
          <pre class="whitespace-pre-wrap"><%= JSON.stringify(error, null, 2) %></pre>
        </div>
      </div>
    <% } %>

    <div class="mt-8 text-center">
      <a href="/" class="text-indigo-600 hover:text-indigo-500">
        العودة إلى الصفحة الرئيسية
      </a>
    </div>
  </div>
</div> 